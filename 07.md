# SUBPROMPT 7 — Full End-to-End Simulation

Implement scripts/simulate.py:
- start committee nodes
- deploy contract
- create rid=1
- for i in 1..20:
  - choose s_i (deterministic, e.g., i%51) and random r_i
  - compute commitment and submitVote
  - shamir split (s_i,r_i) and POST shares to committee nodes
- trigger aggregation:
  - request t committee nodes for their (S_share_k,R_share_k) and partial sig
  - reconstruct S,R using Shamir reconstruction from shares
  - aggregate BLS sigma over message m=H(...)
  - call submitAggregate
- read contract state and verify it matches expected

Print:
- expected S and R
- contract verification success
- weight = S/voteCount
- doc text from CSV

Ensure `RUN.md` includes exact commands.